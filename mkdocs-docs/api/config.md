# Configuration

Configuration dataclasses with validation, merging, and serialization. Each config supports `from_dict()`, `from_yaml()`, `merge()`, `to_dict()`, and `to_yaml()`.

::: rlox.config.PPOConfig

::: rlox.config.SACConfig

::: rlox.config.DQNConfig
